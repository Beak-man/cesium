/*global define*/
define([
        '../createCommand',  
        '../../Core/defined',
        '../../Core/defineProperties',
        '../../ThirdParty/knockout'
    ], function(
	    createCommand,
        defined,
        defineProperties,
        knockout) {
    "use strict";

   /**
     * The view model for {@link Tools}.
     * @alias ToolsViewModel
     * @constructor
     */
    var ToolsViewModel = function(container, viewer){
	
	
	};

    return ToolsViewModel;
});
