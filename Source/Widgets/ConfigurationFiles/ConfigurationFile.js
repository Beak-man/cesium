/*global define*/
define([
], function (
        ) {
    "use strict";

    var ConfigurationFile = function () {

    };

    defineProperties(ConfigurationFile.prototype, {
    });

    return ConfigurationFile;
});